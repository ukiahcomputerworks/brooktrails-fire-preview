# Brooktrails Fire Department concept preview

Static single-page prospect mockup. No build step is required. The local `assets` folder contains the Brooktrails station image used in the hero.

Open `index.html` directly or serve the directory with any static web server. See `DESIGN-NOTES.md` for the visual system, content boundaries, image provenance, and source links.
