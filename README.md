# Brooktrails Fire Department concept preview

Static single-page prospect mockup. No build step or external assets are required.

Open `index.html` directly or serve the directory with any static web server. The request demo is intentionally non-submitting. See `DESIGN-NOTES.md` for the visual system, content boundaries, route matrix, and source links.
